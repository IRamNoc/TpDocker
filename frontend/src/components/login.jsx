import React from "react";
const apiUri = process.env.API_URL;

const Login = () => {
  let loading = false;
  const handleChaange = () => { };
  const handleSubmit = () => { };
  return (
    <div>Connexion</div>
  );
};

export default Login;
